# Issue audit: #34, #36-#44

| Issue | Verdict | Justification |
|---|---|---|
| #34 | STILL OPEN (ready-for-human) | `web/package.json:16` still `react-router-dom@^7.18.2` (vulnerable range); `.github/workflows/security.yml` still allowlists `GHSA-qwww-vcr4-c8h2` pending human decision. Facts unchanged. |
| #36 | STILL OPEN (ready-for-human) | `src/meeting_mgr/api/auth.py:94-96` still auto-provisions OIDC subjects into `Organization(name="default")` with `role="member"`. Documented as an operator trust boundary in `docs/security.md:47-68`, but no org-to-claim mapping exists. Decision item, not implemented. |
| #37 | STILL OPEN (ready-for-human) | Architecture unchanged: `get_session`/`get_readonly_session` (superuser, RLS-bypassing) still used at the 3 identity-bootstrap call sites (`api/auth.py:42`, `auth/deps.py:38`, both comment-tagged "see issue #37"). No narrow-privilege role introduced. Decision item. |
| #38 | RESOLVED | `src/meeting_mgr/chunk_storage.py` created by 980bc18. Both `api/capture.py:7,18-27` and `api/bot.py:11,90-99` call the shared `chunk_key`/`chunk_prefix`/`chunk_seq`. Both ordering guards intact: `{seq:06d}` zero-pad in `chunk_key()` and numeric sort key at call sites (`capture.py:170`, `bot.py:209`). Distinct prefixes preserved (`_SUBDIR="chunks"` implied vs `bot.py:17 _SUBDIR="bot-chunks"`) and manifest filenames unchanged (`manifest.json` vs `bot-manifest.json`). |
| #39 | STILL OPEN | `src/meeting_mgr/models/bot.py:18-26` `BotCredential` has no `__table_args__`/`UniqueConstraint`, unlike `WatchFolder`'s `uq_watch_folder_org_path` (`models/ingest.py:17-19`). Migration `migrations/versions/a2c7f4e91b3d_add_bot_credential.py` creates no such constraint either. |
| #40 | STILL OPEN | No clarifying comment added at either manifest writer: `api/capture.py:219` (`manifest.json`) or `api/bot.py:231` (`bot-manifest.json`). Proposed one-line-comment resolution not done. |
| #41 | STILL OPEN | `src/meeting_mgr/bot_credentials.py:31` and `:33` — `s.flush()` still called twice in `create_bot_credential`. Commit fbbe7e2 removed the unrelated dead `get_bot_credential_by_id` helper, not the double flush. |
| #42 | STILL OPEN | Verified independently: `migrations/versions/0001_initial.py:127` seeds only an `Organization`, no `Account`. `api/auth.py:96` OIDC auto-provision always sets `role="member"`. `POST /auth/login` (`api/auth.py:39-57`) only matches existing Accounts, never creates one; mTLS likewise. `docs/admin-guide.md:90-141` documents the manual-SQL workaround explicitly as "a product gap, not an intended workflow" — documentation of the workaround does not resolve the underlying gap; no bootstrap code exists. |
| #43 | STILL OPEN (partially mitigated, as issue itself notes) | Commit 16744aa made `finish_capture` retryable via conditional `UPDATE ... WHERE status='finishing'` (`api/capture.py`), matching the issue's own "partially mitigated already" description. But no `src/meeting_mgr/pipeline/capture.py` sweep module exists, no `Meeting.updated_at` column (`models/meeting.py` has only `created_at`), and no reclaim path for a stranded "capturing" Meeting. Core ask (reclaim sweep) not implemented. |
| #44 | STILL OPEN | No re-dispatch sweep for Meetings stuck in `"pending"` exists anywhere (checked `pipeline/*.py`, `api/*.py` for any sweep of pending meetings — none found). Commit 16744aa's own message states this gap is "noted but not fixed here -- out of scope, flagged for follow-up," consistent with current code. |
